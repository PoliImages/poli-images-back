name: poli_images_back
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  cpf_cnpj_validator: ^2.0.0
  crypt: ^4.3.1
  dotenv: ^4.2.0
  mongo_dart: ^0.10.5
  path: ^1.9.0
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
